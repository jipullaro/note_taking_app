# One deployment now builds BOTH services (see `services` in vercel.json), so
# neither frontend/ nor backend/ may be listed here — excluding `backend/`
# was correct when the Django app was a separate project and would silently
# empty the backend service now.
#
# What's left is the local-development surface, which is dead weight in the
# upload and part of no service's build.
assets/
docker-compose.yml
Makefile
.pre-commit-config.yaml
