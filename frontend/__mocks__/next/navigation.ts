// Vitest resolves `vi.mock("next/navigation")` to this file. The implementation
// lives in src/test/ so tests can import its helpers via the "@/" alias without
// reaching up into __mocks__.
export * from "../../src/test/next-navigation";
